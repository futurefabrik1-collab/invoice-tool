{
  "name": "invoice-tool",
  "version": "1.0.0",
  "description": "Invoice maker tool for Future Fabrik",
  "scripts": {
    "dev": "concurrently \"python backend/app.py\" \"cd frontend && npm run dev\"",
    "install-all": "pip install -r requirements.txt && cd frontend && npm install"
  },
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
